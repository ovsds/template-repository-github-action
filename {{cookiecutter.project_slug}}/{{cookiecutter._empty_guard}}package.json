{
  "private": true,
  "name": "{{cookiecutter.project_slug}}",
{%- if cookiecutter.action_type == 'node' %}
  "main": "lib/main.js",
  "dependencies": {
    "@actions/core": "2.0.1"
  },
{%- endif %}
  "devDependencies": {
{%- if cookiecutter.action_type == 'node' %}
    "@types/node": "^25.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@vercel/ncc": "^0.38.0",
    "eslint": "^9.0.0",
    "vitest": "^4.0.0",
{%- endif %}
    "@commitlint/cli": "^20.0.0",
    "@commitlint/config-conventional": "^20.0.0",
    "husky": "^9.0.0",
    "lint-staged": "^16.0.0",
    "prettier": "^3.0.0"
  },
  "hooks": {
    "pre-commit": "lint-staged",
    "commit-msg": "commitlint"
  },
  "lint-staged": {
{%- if cookiecutter.action_type == 'node' %}
    "*.{ts,tsx}": [
      "prettier --check",
      "eslint"
    ],
{%- endif %}
    "*.{md,yaml,json,js}": [
      "prettier --check"
    ]
  },
  "scripts": {
    "lint:check": "npm run lint:check:prettier{% if cookiecutter.action_type == 'node' %} && npm run lint:check:eslint{% endif %}",
{%- if cookiecutter.action_type == 'node' %}
    "lint:check:eslint": "eslint .",
{%- endif %}
    "lint:check:prettier": "prettier --check .",
    "lint:fix": "npm run lint:fix:prettier{% if cookiecutter.action_type == 'node' %} && npm run lint:fix:eslint{% endif %}",
{%- if cookiecutter.action_type == 'node' %}
    "lint:fix:eslint": "eslint . --fix",
{%- endif %}
    "lint:fix:prettier": "prettier --write .",
{%- if cookiecutter.action_type == 'node' %}
    "test": "vitest",
    "test:unit": "vitest tests/unit",
    "test:integration": "vitest tests/integration",
    "build": "tsc",
    "package": "rm -rf ./lib ./dist && npm run build && ncc build",
{%- endif %}
    "prepare": "husky"
  }
}
